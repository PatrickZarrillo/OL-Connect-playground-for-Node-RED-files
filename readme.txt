placeholder

3)	Open the Startup tab and open the Set Globals node (change node). Update the workspace and email parameters to match your setup.
4)	Deploy the flow. This should automatically load the manifest file, set the globals and upload the required Connect resources to the OL Connect server.
